# Backend (NestJS) manzili — sinxronizatsiya uchun. Production da VPS URL qo'yiling.
BACKEND_URL=http://localhost:3000

# POS desktop login/auth uchun backend manzili (build vaqtida — Vite shu qiymatni .env dan o'qiydi).
# Bu faylni .env nomi bilan nusxalang (copy .env.example .env) va quyidagi manzilni o'zingiznikiga o'zgartiring.
VITE_API_URL=https://nadpos.uz/api

# Ixtiyoriy: migratsiya/test uchun. Odatda login orqali settings ga yoziladi.
# RESTAURANT_ID=
